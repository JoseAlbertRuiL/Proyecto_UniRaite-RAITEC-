generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

/*Modelo*/
model User {
  id       Int    @id @default(autoincrement())
  email    String @unique
  password String
  name     String?
}